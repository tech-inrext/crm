"use client";

import React from "react";
import PageHeader from "@/fe/framework/components/PageHeader";

const VendorBookingPageHeader: React.FC = () => (
  <PageHeader
    title="Vendor Bookings"
    subtitle="Your assigned cab bookings — fill in details for active bookings"
  />
);

export default VendorBookingPageHeader;

import React from "react";
import { Box, Typography } from "@mui/material";

const LeadAnalytics = () => {
  return (
    <Box>
      <Typography variant="h5">Lead Analytics</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        This section will contain analytics related to Leads.
      </Typography>
    </Box>
  );
};

export default LeadAnalytics;
